import { NextRequest, NextResponse } from "next/server";
import * as XLSX from "xlsx";
import { db } from "@/lib/db";
import { requireAdmin } from "@/lib/auth";
const text=(value:unknown)=>String(value??"").trim();
export async function POST(request:NextRequest){ try { await requireAdmin(); const form=await request.formData(); const file=form.get("file"); if(!(file instanceof File)) throw new Error("Choose an .xlsx backup first"); const book=XLSX.read(Buffer.from(await file.arrayBuffer()),{type:"buffer",cellDates:true}); const lifetime=XLSX.utils.sheet_to_json<Record<string,unknown>>(book.Sheets["Lifetime Leaderboard"]||{}); const categories=XLSX.utils.sheet_to_json<Record<string,unknown>>(book.Sheets["Category Leaderboards"]||{}); const pokemon=XLSX.utils.sheet_to_json<Record<string,unknown>>(book.Sheets["Pokemon Records"]||{}); let players=0,records=0; for(const row of lifetime){const ign=text(row.IGN);if(!ign)continue; await db.player.upsert({where:{ign},create:{name:text(row["Full Name"])||ign,ign,points:Number(row["Lifetime Points"])||0,bestPerformance:text(row["Best Performance"])||null},update:{name:text(row["Full Name"])||ign,points:Number(row["Lifetime Points"])||0,bestPerformance:text(row["Best Performance"])||null}});players++;} for(const row of categories){const name=text(row.Category),playerName=text(row.Player); if(!name||!playerName)continue; const category=await db.category.upsert({where:{name},create:{name},update:{}}); const player=await db.player.findFirst({where:{name:playerName,active:true}});if(player){await db.categoryRecord.upsert({where:{categoryId_playerId:{categoryId:category.id,playerId:player.id}},create:{categoryId:category.id,playerId:player.id,wins:Number(row.Wins)||0,losses:Number(row.Losses)||0},update:{wins:Number(row.Wins)||0,losses:Number(row.Losses)||0}});records++;}} for(const row of pokemon){const name=text(row.Category),playerName=text(row.Player);if(!name||!playerName)continue;const [category,player]=await Promise.all([db.category.findUnique({where:{name}}),db.player.findFirst({where:{name:playerName,active:true}})]);if(category&&player){const roster=[1,2,3,4,5,6].map(i=>text(row[`Pokemon ${i}`])).filter(Boolean);await db.categoryRecord.upsert({where:{categoryId_playerId:{categoryId:category.id,playerId:player.id}},create:{categoryId:category.id,playerId:player.id,pokemon:JSON.stringify(roster)},update:{pokemon:JSON.stringify(roster)}})}} return NextResponse.json({ok:true,message:`Imported ${players} players and ${records} battle records.`}); }catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Import failed"},{status:400});}}

